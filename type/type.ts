export type stat = {
  stat: {
    name: string;
  };
  base_stat: number;
};

export type move = {
  move: {
    name: string;
  };
};

export type pokemons = {
  name: string;
  id: number;
  image: string;
  dreamworld: string;
};
